# OpenAI (for writer + engagement predictor)
OPENAI_API_KEY=sk-your-openai-key

# X (Twitter) API — from developer portal
X_API_KEY=your-consumer-key
X_API_SECRET=your-consumer-secret
X_ACCESS_TOKEN=your-access-token
X_ACCESS_SECRET=your-access-token-secret
# For one-time OAuth with ngrok (see README): set to https://YOUR-NGROK-URL/callback
# X_CALLBACK_URL=https://your-ngrok-subdomain.ngrok-free.app/callback

# Optional: tune posting behavior
# MATCH_LOOP_SECONDS=30
# MIN_POST_DELAY=5
# MAX_POST_DELAY=25
